<% if (post.tags && post.tags.length){ %>
	<ul class="article-tag-list">
		<% post.tags.forEach(function(tag, i) {%>
			<li class="article-tag-list-item">
				<a href="/tags/<%-tag.name%>/" class="article-tag-list-link"><%-tag.name%></a>
			</li>
		<% }) %>
	</ul>
<% } %>